
before_script:
    - export GRADLE_USER_HOME=`pwd`/.gradle

cache:
  paths:
    - .gradle/wrapper
    - .gradle/caches

stages:
  - build
  - test

build:
  script: gradle assemble
  stage: build

test:
  script: gradle check --info
  stage: test
